# Backend/API (servidor Node/Express para checkout y contacto)
VITE_API_URL=http://localhost:3000

# Supabase (auth, DB, storage). En el dashboard: Project Settings → API → URL y anon public key.
# Solo usar la anon key en el frontend; nunca la service_role.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

